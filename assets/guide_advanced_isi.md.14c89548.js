import{_ as e,o as t,c as o,d as a}from"./app.22d6dd61.js";const f=JSON.parse('{"title":"Iroha Special Instructions","description":"","frontmatter":{},"headers":[{"level":2,"title":"How Iroha works","slug":"how-iroha-works"},{"level":3,"title":"Consensus","slug":"consensus"},{"level":2,"title":"Instructions","slug":"instructions"},{"level":3,"title":"(Un)Register","slug":"un-register"},{"level":3,"title":"Mint/Burn","slug":"mint-burn"},{"level":3,"title":"Grant/Revoke","slug":"grant-revoke"},{"level":3,"title":"SetKeyValue/RemoveKeyValue","slug":"setkeyvalue-removekeyvalue"},{"level":3,"title":"Query","slug":"query"},{"level":3,"title":"Expressions, Conditionals, Logic","slug":"expressions-conditionals-logic"}],"relativePath":"guide/advanced/isi.md","lastUpdated":1659428266000}'),s={name:"guide/advanced/isi.md"},n=a('<h1 id="iroha-special-instructions" tabindex="-1">Iroha Special Instructions <a class="header-anchor" href="#iroha-special-instructions" aria-hidden="true">#</a></h1><p>To understand how Iroha operates, let&#39;s draw parallels between a blockchain and a computer. If the blockchain is the computer, then in this metaphor of ours the client binary (for example: <code>iroha_client_cli</code>) is the keyboard, the blockchain is the hard drive, and the Iroha peer software is the processor. Like a processor, Iroha accepts portable instructions that modify what&#39;s written to the blockchain, allow certain users to use the network, and lock others out.</p><p>Any operation that is run on-chain is written in terms of <em>Iroha Special Instructions (ISI)</em>, and there is no other way of modifying the world state. To understand why, we&#39;ll need to make a short detour into how Iroha is implemented under the hood.</p><h2 id="how-iroha-works" tabindex="-1">How Iroha works <a class="header-anchor" href="#how-iroha-works" aria-hidden="true">#</a></h2><p>Each interaction with the blockchain is done via a <em>transaction</em>. A transaction is a collection of <em>instructions</em>, which are either glued together in sequence or compiled into what we affectionately call a WASM blob (more on that later). You need these instructions to register an account, remove an account, add X amount of Y currency, and so on.</p><p>To read the information encoded in the blocks of a blockchain (the current world state), you use <em>queries</em>. Queries are submitted like instructions, but they&#39;re not tracked and recorded in blocks, so they&#39;re much more lightweight. If you use queries as part of complicated logic (e.g. inside WASM), they have a non-negligible impact on the size of the blocks. Queries that are only used to get information leave no trace in the blockchain.</p><h3 id="consensus" tabindex="-1">Consensus <a class="header-anchor" href="#consensus" aria-hidden="true">#</a></h3><p>Each time you send a transaction to Iroha, it gets put into a queue. When it&#39;s time to produce a new block, the queue is emptied, and the consensus process begins. This process is equal parts common sense and black magic<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup>.</p><p>The mundane aspect is that a special set of peers needs to take the transaction queue and reproduce the same world state. If the world state cannot be reproduced for some reason or another, none of the transactions get committed to a block.</p><p>The consensus starts over from scratch by choosing a different special set of peers. This is where the black magic comes in. There is a number of things that are fine-tuned: the number of peers in the voting process, the way in which subsequent voting peers are chosen, and the way in which the peers communicate that consensus has failed. Because this changes the view of the world, the process is called a <em>view change</em>. The exact reason for why the view was changed is encoded in the <em>view change proof</em>, but decoding that information is an advanced topic that we won&#39;t cover here.</p><p>The reasoning behind this algorithm is simple: if someone had some evil peers and connected them to the existing network, if they tried to fake data, some good\u2122 peers would not get the same (evil\u2122) world state. If that&#39;s the case, the evil\u2122 peers would not be allowed to participate in consensus, and you would eventually produce a block using only good\u2122 peers.</p><p>As a natural consequence, if any changes to the world state are made without the use of ISI, the good\u2122 peers cannot know of them. They won&#39;t be able to reproduce the hash of the world state, and thus consensus will fail. Same thing happens if the peers have different instructions.</p><h2 id="instructions" tabindex="-1">Instructions <a class="header-anchor" href="#instructions" aria-hidden="true">#</a></h2><p>So what kinds of special instructions do we have? If you&#39;ve read the tutorial for <a href="./../rust.html">Rust</a> or <a href="./../python.html">Python</a>, you&#39;ve already seen a couple of instructions: <code>Register&lt;Account&gt;</code> and <code>Mint&lt;Quantity&gt;</code>.</p><p>For the exhaustive list of Instructions you should consult <a href="https://github.com/hyperledger/iroha/tree/iroha2-dev/core/src/smartcontracts/isi" target="_blank" rel="noopener noreferrer">our source code</a>.</p><p>Here we will cover only some important classes.</p><h3 id="un-register" tabindex="-1">(Un)Register <a class="header-anchor" href="#un-register" aria-hidden="true">#</a></h3><p>Registering and unregistering are the instructions used to give an ID to a new entity on the blockchain.</p><p>Everything that can be registered is both <code>Registrable</code> and <code>Identifiable</code>, but not everything that&#39;s <code>Identifiable</code> is <code>Registrable</code>. Most things are registered directly, like <code>Peer</code>s, but in some cases the representation in the blockchain has considerably more data. For security and performance reasons, we use builders for such data structures (e.g. <code>NewAccount</code>). As a rule, everything that can be registered, can also be un-registered, but that is not a hard and fast rule.</p><p>You can register domains, accounts, asset definitions, peers, roles, and triggers (more on them later).</p><p>Registering a peer is currently the only way of adding peers that were not part of the original <code>TRUSTED_PEERS</code> array to the network.</p><p>Registering an account is different. Iroha can be compiled in two modes: <em>public</em> and <em>private</em>. If it&#39;s compiled with <em>private</em> permissions, to register an account, you need an account. This is the default. If you want your users to be able to register without access to a pre-existing account, you need to compile Iroha in the <em>public</em> mode.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>As of writing, the set of public blockchain permissions is incomplete, and as such Iroha source code needs to be modified to run it in the <em>public</em> mode.</p></div><h3 id="mint-burn" tabindex="-1">Mint/Burn <a class="header-anchor" href="#mint-burn" aria-hidden="true">#</a></h3><p>Minting and burning can refer to assets, triggers (if the trigger has a limited number of repetitions), and temporary permission tokens. Some assets can be declared as non-mintable, meaning that they can be minted only once after registration.</p><p>Assets and permission tokens need to be minted to a specific account, usually the one that registered the asset in the first place. All assets are assumed to be non-negative as well, so you can never have <code>-1.0</code> of <code>time</code> or <code>Burn</code> a negative amount and get a <code>Mint</code>.</p><h3 id="grant-revoke" tabindex="-1">Grant/Revoke <a class="header-anchor" href="#grant-revoke" aria-hidden="true">#</a></h3><p>These are used for permissions and roles.</p><p>When minting a permission token, a user is granted <code>X</code> amount of permissions to do <code>Y</code>, and the token will expire once all <code>X</code> operations were performed.</p><p>By contrast, the <code>Grant</code> operation can be used to permanently grant a user either a single permission, or a group of permissions (a &quot;role&quot;). As such these instructions should be used carefully.</p><h3 id="setkeyvalue-removekeyvalue" tabindex="-1"><code>SetKeyValue</code>/<code>RemoveKeyValue</code> <a class="header-anchor" href="#setkeyvalue-removekeyvalue" aria-hidden="true">#</a></h3><p>These instructions are used with the key/value <code>Store</code> asset type. This use case has not received much attention so far, because storing data in the blockchain is a rather advanced topic that we shall cover separately.</p><h3 id="query" tabindex="-1">Query <a class="header-anchor" href="#query" aria-hidden="true">#</a></h3><p>We talk extensively about queries in the <a href="./queries.html">dedicated section</a> where all the queries that can be made from the client-side are listed. This is not necessarily the only kind of information that is available on the network, but it&#39;s the only kind of information that is <em>guaranteed</em> to be accessible on all networks.</p><p>For each deployment of Iroha, there might be other available information. For example, the availability of telemetry data is up to the network administrators. It&#39;s entirely their decision whether or not they want to allocate processing power to track the work instead of using it to do the actual work. By contrast, some functions are always required, e.g. having access to your account balance.</p><h3 id="expressions-conditionals-logic" tabindex="-1">Expressions, Conditionals, Logic <a class="header-anchor" href="#expressions-conditionals-logic" aria-hidden="true">#</a></h3><p>All ISI operate on expressions. Each expression has an <code>EvaluatesTo</code>, which is used in instruction execution. While you could specify the account name directly, you could also specify the account ID via some mathematical or string operation. You can check if an account is registered on the blockchain too.</p><p>Using expressions that implement <code>EvaluatesTo&lt;bool&gt;</code>, you can set up conditional logic and execute more sophisticated operations on-chain. For example, you can submit a <code>Mint</code> instruction only if a specific account is registered.</p><p>Recall that you can combine this with queries, and as such can program the blockchain to do some amazing stuff. This is what we refer to as <em>smart contracts</em>, the defining feature of the advanced usage of blockchain technology.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>For prospective wizards, the <a href="https://github.com/hyperledger/iroha/blob/iroha2-dev/docs/source/iroha_2_whitepaper.md" target="_blank" rel="noopener noreferrer">Iroha 2 Whitepaper</a> is a good start. <a href="#fnref1" class="footnote-backref">\u21A9\uFE0E</a></p></li></ol></section>',41),i=[n];function r(c,h,d,l,u,p){return t(),o("div",null,i)}var g=e(s,[["render",r]]);export{f as __pageData,g as default};
