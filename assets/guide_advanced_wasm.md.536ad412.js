import{_ as s,o as e,c as a,d as n}from"./app.22d6dd61.js";const m=JSON.parse('{"title":"WASM","description":"","frontmatter":{},"headers":[{"level":2,"title":"Working with WASM","slug":"working-with-wasm"},{"level":2,"title":"Simple Rust Smart Contract Example","slug":"simple-rust-smart-contract-example"},{"level":3,"title":"1. Create a new project","slug":"_1-create-a-new-project"},{"level":3,"title":"2. Write a smart contract","slug":"_2-write-a-smart-contract"},{"level":2,"title":"Advanced Smart Contracts: Optimising for Size","slug":"advanced-smart-contracts-optimising-for-size"},{"level":3,"title":"Remove debugging info","slug":"remove-debugging-info"},{"level":3,"title":"Work under a no_std environment","slug":"work-under-a-no-std-environment"},{"level":3,"title":"Re-compile libcore","slug":"re-compile-libcore"},{"level":3,"title":"Use tools to optimise WASM size","slug":"use-tools-to-optimise-wasm-size"},{"level":3,"title":"Conclusion","slug":"conclusion"}],"relativePath":"guide/advanced/wasm.md","lastUpdated":1659428266000}'),o={name:"guide/advanced/wasm.md"},t=n(`<h1 id="wasm" tabindex="-1">WASM <a class="header-anchor" href="#wasm" aria-hidden="true">#</a></h1><p>While we had initially assumed that all operations within Iroha will be handled with instructions and conditionals, however, there are a few problems with this approach.</p><ul><li>The ISI syntax is verbose and ugly.</li><li>The ISI syntax is not familiar for most programmers.</li><li>While simple ISI smart contracts are compact (usually a few bytes), they need different kinds of be manually optimised.</li></ul><p>In the long run, all of these problems are taken care of by using a <strong>domain-specific language</strong>, which gets optimised and compiled into a sequence of instructions that executes fast and takes very little space in the blocks, but is also easy to understand. Something that looks like your traditional <code>if</code> statements and <code>for</code> loops.</p><p>However, in the interim, we have decided to use another portable binary standard called <strong>Web assembly</strong>, or <strong>WASM</strong>.</p><h2 id="working-with-wasm" tabindex="-1">Working with WASM <a class="header-anchor" href="#working-with-wasm" aria-hidden="true">#</a></h2><p>The main advantage of using the WASM format is that you can use any language you like (as long as it links statically against our helper library), and produce a 32-bit portable executable. The compilers take care of the optimisation, and you don&#39;t have to learn a new language (ahem... solidity... ahem), just to operate on the blockchain.</p><p>You&#39;d still need to use ISI from inside your WASM binary to do anything on-chain, as we explained earlier.</p><p>In theory, you can do anything you want just using ISI as it is a Turing-complete language. However, it&#39;ll be less convenient and efficient since you&#39;d need to use metadata as memory and write complex conditionals using just the tools that we&#39;ve provided in the <code>Expression</code> and ISI infrastructure. We highly recommend choosing a well-known programming language, such as Rust, to build the necessary logic out of simple instructions. This is much easier than trying to reinvent the wheel using ISI.</p><p>Moreover, as long as you fit within the limits of WASM runtime and the provided libraries, you can do anything (and everything) you want. The drawback is that this process is a tad more involved than just writing the ISI using the client libraries.</p><h2 id="simple-rust-smart-contract-example" tabindex="-1">Simple Rust Smart Contract Example <a class="header-anchor" href="#simple-rust-smart-contract-example" aria-hidden="true">#</a></h2><p>WASM projects, just like any other binary in Rust, need to be separate crates. Don&#39;t worry, it doesn&#39;t have to be big.</p><h3 id="_1-create-a-new-project" tabindex="-1">1. Create a new project <a class="header-anchor" href="#_1-create-a-new-project" aria-hidden="true">#</a></h3><p>To get started, create a new project:</p><div class="language-bash"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">cargo new --lib</span></span>
<span class="line"></span></code></pre></div><p>Yes! We need the <code>lib</code> type; more on that later.</p><p>The <code>Cargo.toml</code> of your project should look something like this:</p><div class="language-toml"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">[</span><span style="color:#F69D50;">package</span><span style="color:#ADBAC7;">]</span></span>
<span class="line"><span style="color:#ADBAC7;">name  = </span><span style="color:#96D0FF;">&quot;smartcontract&quot;</span></span>
<span class="line"><span style="color:#ADBAC7;">version = </span><span style="color:#96D0FF;">&quot;0.1.0&quot;</span></span>
<span class="line"><span style="color:#ADBAC7;">edition = </span><span style="color:#96D0FF;">&quot;2021&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#ADBAC7;">[</span><span style="color:#F69D50;">lib</span><span style="color:#ADBAC7;">]</span></span>
<span class="line"><span style="color:#ADBAC7;">crate-type = [</span><span style="color:#96D0FF;">&#39;cdylib&#39;</span><span style="color:#ADBAC7;">]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#ADBAC7;">[</span><span style="color:#F69D50;">dependencies</span><span style="color:#ADBAC7;">]</span></span>
<span class="line"><span style="color:#ADBAC7;">iroha_wasm = { git = </span><span style="color:#96D0FF;">&quot;https://github.com/hyperledger/iroha/&quot;</span><span style="color:#ADBAC7;">, branch = </span><span style="color:#96D0FF;">&quot;iroha2-dev&quot;</span><span style="color:#ADBAC7;"> }</span></span>
<span class="line"></span></code></pre></div><p>Note that the crate type is <code>cdylib</code>. Most Rust code is linked in a non-portable architecture and OS-specific static manner, but WASM is a portable format. Since C ABI is the <em>lingua franca</em> of the programming world and there is no other stable Rust ABI (yet), Iroha relies on the C-linkage to generate WASM bindings. Thankfully, <code>iroha_wasm</code> takes care of everything related to foreign function interfaces (FFI), so you don&#39;t have to worry about things like <code>unsafe</code>, <code>repr(C)</code>, padding, alignment, and others.</p><p>The <code>iroha_wasm</code> crate contains all of the bindings, macros, and trait implementations that you&#39;d need to write the program, most notably the <code>iroha_wasm</code> attribute macro. The crate also exposes our <code>data_model</code>, which contains all of the basic ISI and types. The chosen serialisation format is <code>parity-scale-codec</code>, though there is a strong possibility it&#39;ll get replaced with a different (custom) serialisation format in the near future, as it seems to dominate the binary size<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup>.</p><h3 id="_2-write-a-smart-contract" tabindex="-1">2. Write a smart contract <a class="header-anchor" href="#_2-write-a-smart-contract" aria-hidden="true">#</a></h3><p>Now that we have the preliminaries nailed down, we get to write some code for our smart contract. In the <code>src/lib.rs</code> you should write the following:</p><div class="language-rust"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">#</span><span style="color:#F47067;">!</span><span style="color:#ADBAC7;">[no_std]</span></span>
<span class="line"><span style="color:#ADBAC7;">#</span><span style="color:#F47067;">!</span><span style="color:#ADBAC7;">[no_main]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F47067;">extern</span><span style="color:#ADBAC7;"> </span><span style="color:#F47067;">crate</span><span style="color:#ADBAC7;"> alloc;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F47067;">use</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">alloc</span><span style="color:#F47067;">::</span><span style="color:#F69D50;">vec</span><span style="color:#F47067;">::</span><span style="color:#F69D50;">Vec</span><span style="color:#ADBAC7;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F47067;">use</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">iroha_wasm</span><span style="color:#F47067;">::</span><span style="color:#F69D50;">data_model</span><span style="color:#F47067;">::</span><span style="color:#F69D50;">prelude</span><span style="color:#F47067;">::*</span><span style="color:#ADBAC7;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#ADBAC7;">#[iroha_wasm</span><span style="color:#F47067;">::</span><span style="color:#ADBAC7;">iroha_wasm]</span></span>
<span class="line"><span style="color:#F47067;">fn</span><span style="color:#ADBAC7;"> </span><span style="color:#DCBDFB;">smartcontract_entry_point</span><span style="color:#ADBAC7;">(_account_id</span><span style="color:#F47067;">:</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">AccountId</span><span style="color:#ADBAC7;">) {</span></span>
<span class="line"><span style="color:#ADBAC7;">    </span><span style="color:#F47067;">let</span><span style="color:#ADBAC7;"> query </span><span style="color:#F47067;">=</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">QueryBox</span><span style="color:#F47067;">::</span><span style="color:#DCBDFB;">FindAllDomains</span><span style="color:#ADBAC7;">(</span><span style="color:#F69D50;">FindAllDomains</span><span style="color:#ADBAC7;"> {});</span></span>
<span class="line"><span style="color:#ADBAC7;">    </span><span style="color:#F47067;">let</span><span style="color:#ADBAC7;"> domains</span><span style="color:#F47067;">:</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">Vec</span><span style="color:#ADBAC7;">&lt;</span><span style="color:#F69D50;">Domain</span><span style="color:#ADBAC7;">&gt; </span><span style="color:#F47067;">=</span><span style="color:#ADBAC7;"> query</span><span style="color:#F47067;">.</span><span style="color:#DCBDFB;">execute</span><span style="color:#ADBAC7;">()</span><span style="color:#F47067;">.</span><span style="color:#DCBDFB;">try_into</span><span style="color:#ADBAC7;">()</span><span style="color:#F47067;">.</span><span style="color:#DCBDFB;">unwrap</span><span style="color:#ADBAC7;">();</span></span>
<span class="line"></span>
<span class="line"><span style="color:#ADBAC7;">    </span><span style="color:#F47067;">for</span><span style="color:#ADBAC7;"> domain </span><span style="color:#F47067;">in</span><span style="color:#ADBAC7;"> domains {</span></span>
<span class="line"><span style="color:#ADBAC7;">        </span><span style="color:#F47067;">let</span><span style="color:#ADBAC7;"> new_account_id </span><span style="color:#F47067;">=</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">AccountId</span><span style="color:#ADBAC7;"> {</span></span>
<span class="line"><span style="color:#ADBAC7;">            name</span><span style="color:#F47067;">:</span><span style="color:#ADBAC7;"> </span><span style="color:#F69D50;">Name</span><span style="color:#F47067;">::</span><span style="color:#DCBDFB;">new</span><span style="color:#ADBAC7;">(</span><span style="color:#96D0FF;">&quot;mad_hatter&quot;</span><span style="color:#ADBAC7;">)</span><span style="color:#F47067;">.</span><span style="color:#DCBDFB;">unwrap</span><span style="color:#ADBAC7;">(),</span></span>
<span class="line"><span style="color:#ADBAC7;">            domain_id</span><span style="color:#F47067;">:</span><span style="color:#ADBAC7;"> domain</span><span style="color:#F47067;">.</span><span style="color:#ADBAC7;">id,</span></span>
<span class="line"><span style="color:#ADBAC7;">        };</span></span>
<span class="line"></span>
<span class="line"><span style="color:#ADBAC7;">        </span><span style="color:#F69D50;">Instruction</span><span style="color:#F47067;">::</span><span style="color:#DCBDFB;">Register</span><span style="color:#ADBAC7;">(</span><span style="color:#F69D50;">RegisterBox</span><span style="color:#F47067;">::</span><span style="color:#DCBDFB;">new</span><span style="color:#ADBAC7;">(</span><span style="color:#F69D50;">NewAccount</span><span style="color:#F47067;">::</span><span style="color:#DCBDFB;">new</span><span style="color:#ADBAC7;">(new_account_id)))</span><span style="color:#F47067;">.</span><span style="color:#DCBDFB;">execute</span><span style="color:#ADBAC7;">();</span></span>
<span class="line"><span style="color:#ADBAC7;">    }</span></span>
<span class="line"><span style="color:#ADBAC7;">}</span></span>
<span class="line"></span></code></pre></div><p>To submit the instruction and run it, execute the following command (be sure to have a peer up):</p><div class="language-"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#adbac7;">cargo run --release</span></span>
<span class="line"><span style="color:#adbac7;"></span></span></code></pre></div><p>What does this smart contract do? Let&#39;s see. It queries all of the currently existing domains and puts the results into a <code>std::vec::Vec</code>. In this case, <code>std::vec::Vec</code> has to be imported from <code>alloc</code>, as we use <code>no_std</code> (more on that <a href="#work-under-a-no-std-environment">later</a>). It is then used to add the user named <code>mad_hatter</code> to all of the existing domains.</p><p>Building the same logic out of <code>Expression</code> and <code>If</code> and <code>Sequence</code> would be significantly harder. Moreover, the actual low-level instructions that would run are very likely not going to be as well-optimised as what the compiler produces.</p><h2 id="advanced-smart-contracts-optimising-for-size" tabindex="-1">Advanced Smart Contracts: Optimising for Size <a class="header-anchor" href="#advanced-smart-contracts-optimising-for-size" aria-hidden="true">#</a></h2><p>WASM smart contracts can get big. So big, in fact, that we might not let you store them in the blockchain. So how do you reduce the size? The most important modifications are done in <code>Cargo.toml</code>:</p><div class="language-toml"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">[</span><span style="color:#F69D50;">profile</span><span style="color:#ADBAC7;">.</span><span style="color:#F69D50;">release</span><span style="color:#ADBAC7;">]</span></span>
<span class="line"><span style="color:#ADBAC7;">strip = </span><span style="color:#96D0FF;">&quot;debuginfo&quot;</span><span style="color:#ADBAC7;"> </span><span style="color:#768390;"># Remove debugging info from the binary</span></span>
<span class="line"><span style="color:#ADBAC7;">panic = </span><span style="color:#96D0FF;">&quot;abort&quot;</span><span style="color:#ADBAC7;">     </span><span style="color:#768390;"># Abort panics as they are transcribed to Traps when compiling for WASM anyways</span></span>
<span class="line"><span style="color:#ADBAC7;">lto = </span><span style="color:#6CB6FF;">true</span><span style="color:#ADBAC7;">          </span><span style="color:#768390;"># Use link-time-optimisation (it produces a notable decrease in binary size)</span></span>
<span class="line"><span style="color:#ADBAC7;">opt-level = </span><span style="color:#96D0FF;">&quot;z&quot;</span><span style="color:#ADBAC7;">     </span><span style="color:#768390;"># Optimise for size vs speed with &quot;s&quot;/&quot;z&quot;(removes vectorization)</span></span>
<span class="line"><span style="color:#ADBAC7;">codegen-units = </span><span style="color:#6CB6FF;">1</span><span style="color:#ADBAC7;">   </span><span style="color:#768390;"># Use one code generation unit (it further reduces the binary size but increases compilation time)</span></span>
<span class="line"></span></code></pre></div><p>Let&#39;s take a closer look at what you can do to reduce the size of the WASM binary.</p><h3 id="remove-debugging-info" tabindex="-1">Remove debugging info <a class="header-anchor" href="#remove-debugging-info" aria-hidden="true">#</a></h3><p>Rust stores a lot of debug information (even when compiled in <code>release</code> mode), which is (as the name suggests) used for debugging a panic in your Rust application. As you would expect, this information increases the size of the compiled WASM significantly.</p><p>Normally, this would be a worthwhile trade-off, but not in our case. Firstly, since the WASM is stored on-chain, it will be permanently recorded in some block and take space on every full node of an Iroha network. Iroha stores a lot of its information in RAM, so storage space for WASM is at a premium.</p><p>Secondly, once the WASM smart contract is stored on-chain, the debug information is no longer accessible. Indeed, you shouldn&#39;t debug on a peer. Instead, you should replicate the conditions that caused the panic locally and debug on your personal machine.</p><h3 id="work-under-a-no-std-environment" tabindex="-1">Work under a <code>no_std</code> environment <a class="header-anchor" href="#work-under-a-no-std-environment" aria-hidden="true">#</a></h3><p>Another step that we&#39;ve already taken involves working under a <code>no_std</code> environment. All of our size-related woes stem from Rust being predominantly statically linked. As such, breaking the standard library into more manageable crates, like using <code>alloc::vec</code> instead of <code>std::vec</code>, can help us reduce the size and compilation time<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup>.</p><h3 id="re-compile-libcore" tabindex="-1">Re-compile <code>libcore</code> <a class="header-anchor" href="#re-compile-libcore" aria-hidden="true">#</a></h3><p>Next, you&#39;re advised to re-compile <code>libcore</code> and any other standard library crate (e.g. <code>alloc</code>) to exclude the leftover panic-related code that comes with the prebuilt <code>core</code> library<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup>:</p><div class="language-bash"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">cargo +nightly build -Z build-std -Z build-std-features=panic_immediate_abort --target wasm32-unknown-unknown</span></span>
<span class="line"></span></code></pre></div><p>Unfortunately, this is an unstable feature. In other words, the developers of the Rust programming language can decide to change how this works, or remove this option entirely.</p><h3 id="use-tools-to-optimise-wasm-size" tabindex="-1">Use tools to optimise WASM size <a class="header-anchor" href="#use-tools-to-optimise-wasm-size" aria-hidden="true">#</a></h3><p>Finally, you can use an automated tool to optimise the size of the WASM binary. You could use <a href="https://github.com/WebAssembly/binaryen" target="_blank" rel="noopener noreferrer"><code>wasm-opt</code></a> or <a href="https://rustwasm.github.io/twiggy/" target="_blank" rel="noopener noreferrer"><code>twiggy</code></a> to guide your manual optimisation efforts.</p><p>We highly advise using <code>wasm-opt</code> because it will often significantly reduce your binary size:</p><div class="language-bash"><span class="copy"></span><pre class="shiki" style="background-color:#22272e;"><code><span class="line"><span style="color:#ADBAC7;">wasm-opt -Os -o output.wasm input.wasm</span></span>
<span class="line"></span></code></pre></div><h3 id="conclusion" tabindex="-1">Conclusion <a class="header-anchor" href="#conclusion" aria-hidden="true">#</a></h3><p>At some point, unfortunately, the smallest size of your WASM blob is going to be determined by the libraries that you need to use. Using all of the above steps on the provided smart contract can reduce it down to a manageable (for the blockchain) size.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Size is an important metric. We shall cover size-optimisation strategies as we go. <a href="#fnref1" class="footnote-backref">\u21A9\uFE0E</a></p></li><li id="fn2" class="footnote-item"><p>It should be noted that excluding the standard library is necessary for compiling to the wasm32 target, and is thus mandatory. <a href="#fnref2" class="footnote-backref">\u21A9\uFE0E</a></p></li><li id="fn3" class="footnote-item"><p><code>wasm-opt</code> can also be used to remove the debug sections. <a href="#fnref3" class="footnote-backref">\u21A9\uFE0E</a></p></li></ol></section>`,49),l=[t];function r(i,p,c,d,h,u){return e(),a("div",null,l)}var A=s(o,[["render",r]]);export{m as __pageData,A as default};
